<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">RestoFlex</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Accueil", root_path, class:"nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Produits", products_path, class:"nav-link active" %>
        </li>
        <li class="nav-item">
          <% if current_user&.cart %>
            <%= link_to "Mon Panier", cart_path(current_user.cart.id), class:"nav-link active" %>
          <% else %>
            <%= link_to "Mon Panier", "#", class:"nav-link active" %>
          <% end %>
        </li>
        <li class="nav-item dropdown">
          <% unless user_signed_in? %>
            <li class="nav-item">
              <%= link_to "Se connecter", new_user_session_path, class:"nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "S'enregistrer", new_user_registration_path, class:"nav-link" %>
            </li>
          <% else %>
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.username %>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><%= link_to "Mon profile", user_profile_path(current_user.user_profile.id), class: "dropdown-item" %></li>
              <li><%= link_to "Mettre à jour mon compte", edit_user_registration_path, class: "dropdown-item" %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to "Se déconnecter", destroy_user_session_path, data: { "turbo-method": :delete } , class: "dropdown-item"%></li>
            </ul>
          <% end %>
        </li>
    </div>
  </div>
</nav>